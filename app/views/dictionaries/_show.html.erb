<div class='infobox'>
	<h4>
		<%= dictionary.name %>
		<%= link_to content_tag(:span, 'Edit'), edit_domain_dictionary_path(@domain, dictionary), :remote => true, :class => 'minibutton makespace-1' %>
		<%= fancy_destroy_button(domain_dictionary_path(@domain, dictionary, :version => dictionary.version), 'All below features will be deleted too. Are you sure?', true) %>
	</h4>

	<div class='body'>
		<table class='plain'>
			<tr>
				<th class='span-3'>Name:</th>
				<td class='span-7'><%= dictionary.name %></td>
				<th class='span-3'>Label:</th>
				<td class='span-7'><%= dictionary.label %></td>
			</tr>
			<tr>
				<th>Hidden:</th>
				<td><%= dictionary.hidden.yesno %></td>
				<th>Last Update:</th>
				<td><%= last_update_msg(dictionary) %></td>
			</tr>
			<tr>
				<th>Description:</th>
				<td colspan="3"><%= dictionary.description %></td>
			</tr>
		</table>
	</div>
</div>